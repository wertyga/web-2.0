
import '../styles/CloudStorage.sass';

const CloudStorage = createReactClass({
    render() {
        return (
            <div id="CloudStorage">

            </div>
        );
    }
});

export default CloudStorage;